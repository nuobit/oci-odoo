#!/usr/bin/env bash
# SPDX-FileCopyrightText: 2026 NuoBiT Solutions, S.L.
# SPDX-License-Identifier: Apache-2.0

set -euo pipefail

if [[ $# -lt 1 ]]; then
    echo "Usage: odoo-shell <database> [extra odoo args...]" >&2
    exit 2
fi

database="$1"
shift

exec odoo-run shell -d "${database}" "$@"
